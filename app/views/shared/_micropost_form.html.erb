<%= form_with(model: @micropost, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "请输入内容..." %>
  </div>
  <%= f.submit "发布", class: "btn btn-primary" %>

<% end %>

